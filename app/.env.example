# Copy this file to .env and fill in the value.
# For local development, use your machine's LAN IP so physical devices can reach it.
# For production (EAS Build), set this in your EAS environment variables.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.x:3001
